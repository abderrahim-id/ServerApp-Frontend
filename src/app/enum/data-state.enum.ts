// this is gonna represent the state of the data
 
export enum DataState {
     // we can make it just like > LOADING, LOADED, ERROR
     // but we need some messages to show to the user
    LOADING_STATE = 'LOADING_STATE', LOADED_STATE = 'LOADED_STATE', ERROR_STATE = 'ERROR_STATE'
    
 }